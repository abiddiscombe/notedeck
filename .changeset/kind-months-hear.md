---
"notedeck": patch
---

Refactors the Button component to remove `duration` class. This resolves an issue where UI components would appear to "flash" in the previous theme color when toggling dark/light mode.
